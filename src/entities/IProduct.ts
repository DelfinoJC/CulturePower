export interface IProduct {
  name: string,
  photo: string,
  description: string
  price: number
}

export interface IProductDTO {
  name: string,
  photo: string,
  description: string,
  price: number
}
